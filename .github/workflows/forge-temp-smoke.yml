name: Forge temporary runner check

on:
  push:
    branches:
      - forge-temp-installer-v026
  pull_request:
    branches:
      - main

permissions:
  contents: read

jobs:
  smoke:
    runs-on: windows-latest
    steps:
      - run: Write-Output "Forge Windows runner is available"
